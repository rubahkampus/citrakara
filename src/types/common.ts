// src/types/common.ts

import mongoose from "mongoose";

// Export the required types
export type ObjectId = mongoose.Types.ObjectId;
export type ISODate = string; // Replace with actual definition if needed
export type Cents = number; // Replace with actual definition if needed